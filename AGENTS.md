# 项目 Agent 规则

这个仓库只处理已保存直播录像的精彩切片，不包含口播讲解或录屏教学工作流。

## 真源位置

- 直播原始录像：`输入/媒体素材/直播录像/`
- 草稿和已确认数据：`工作区/数据/`
- 直播派生母版：`工作区/派生媒体/直播切片/`
- 模板索引：`模板/模板索引.json`
- Remotion 引擎：`引擎/remotion/`
- 最终成片：`输出/最终成片/`

## 必须使用的 Skill 路由

- 直播精彩切片：读取并遵循 `skills/extract-livestream-clips/SKILL.md`。
- 提炼最终内容标题：读取并遵循 `skills/dbs-xhs-title/SKILL.md`。
- 处理任何 Remotion 代码或 CLI 任务：在项目 Skill 之后读取 `skills/remotion-best-practices/SKILL.md`。
- 创建或修改 Remotion React 组件、模板、布局或动画：额外读取 `skills/remotion-markup/SKILL.md`。
- 创建、转换或显示字幕：额外读取 `skills/remotion-captions/SKILL.md`；其中转写建议不得覆盖本项目的本地 Whisper.cpp 边界。
- 渲染检查图、低清预览或最终成片：额外读取 `skills/remotion-render/SKILL.md`，并且只通过本仓库封装命令执行。
- Remotion API、组件属性或 CLI 参数不确定时：读取 `skills/remotion-docs/SKILL.md`，查询当前官方文档后再实现。

## 硬性边界

- 只使用本地 Whisper.cpp，不得调用云端语音转写服务。
- 全片初稿固定使用 `small` 模型，覆盖完整原片时间轴，作为候选评分的可复用证据；不得用文本润色结果替换这份初稿。
- 每个经 Studio 明确批准并锁定的连续区间，必须从其派生母版的原声轨使用 `medium` 模型重新转写。`medium` 只处理批准区间，且不得与 Studio 或渲染并行运行，以保证 16GB 设备可用。
- 最终审校稿必须保留 `small` 与 `medium` 的原始结果及其时间码。本地文本模型必须先扫描全部已批准片段，检测两份结果差异、术语近音、异常重复或断句、敏感表达、人名、数字和上下文语义突变；检测本身不得生成或改写字幕文本。
- 检测到风险时，必须从同一派生母版原声轨截取风险句前后不超过 30 秒的上下文，使用 `large-v3-turbo` 进行第三次本地转写。`large-v3-turbo` 只处理风险窗口，并与其他本地任务顺序执行。
- `small`、`medium` 与 `large-v3-turbo` 一致时自动采纳；存在差异时，本地文本模型只能在已有转写结果和已批准术语表给出的候选中裁决，禁止生成第三种说法、整句改写或补充原声中没有的内容。
- 本地文本模型无法裁决时，最终字幕保留 `medium` 结果，并把全部候选、检测标签、裁决原因和风险写入 `ambiguities`；不得以看似通顺为由猜测改字。
- 原始直播录像不可修改，原直播声音是最终视频唯一主音频来源。
- 每条切片只能映射一个已批准的连续原片区间。
- 唯一允许的播放顺序例外是片内冷开场：从同一批准区间内部复制 2 至 8 秒原画面、原声音和对应字幕到开头，随后从批准区间原起点完整播放；必须记录源时间码和选择理由。
- 直播候选评分必须覆盖 100% 原片时间轴，保存所有已评分区间和淘汰原因；总分严格大于 85 的区间排序去重后全部进入候选，数量不设上限。
- 切点、冷开场和最终视觉都必须在 Studio 经用户明确确认；最终内容标题也必须单独确认，只用于成片文件名。
- 未经确认，不得永久删除原始素材、最终成片、日志或旧文件。

## 验证要求

修改工作流、数据结构、Skill 或模板后，运行 `npm run check`。修改视觉模板后，还要渲染代表性检查图并实际查看。

## 运行时资源调优

- 转录线程和最终 Remotion 渲染并发必须由 `scripts/platform.mjs` 的 `runtimeTuning()` 根据当前可用处理器与内存计算；不得把某台机器的线程数、并发数或内存档位直接写入工作流脚本。
- 所有 Whisper.cpp 转录，包括开头、中段、结尾预检，必须使用同一份运行时计算出的线程数。资源调优不得改变 `small`、`medium`、`large-v3-turbo` 的适用范围、顺序执行边界或字幕质量校验。
- 最终 Remotion 渲染使用运行时计算出的并发，并仅以 `if-possible` 请求硬件编码。Windows 上 AMD 显卡不会被 Remotion 内建 NVENC 路径选中；未单独验证 FFmpeg AMF 链路前，不得假定 AMD 可用于最终 H.264 编码。
- 需要进一步提高某类模板的吞吐量时，先对代表性已批准切片做基准测试并记录结果；调整资源计算公式和测试，不得只为当前机器写死新参数。

Windows 本地转录使用 Whisper.cpp 1.5.5 时不得开启 DTW token 时间码；Windows 使用句段字幕，macOS 和 Linux 可保留 token 时间码。

修改 Windows Whisper 二进制、模型、转录参数或字幕后，在启动整片转录前，必须抽测原片开头、中段和结尾各 30 秒。项目实际消费的标准化字幕必须没有 `U+FFFD` 字符、无效时间码或超过 30 秒的单条字幕；任一项失败时，不得启动整片转录或候选评分。

`medium` 复转写和 `large-v3-turbo` 风险窗口转写均不构成新的整片转录，但其每份标准化结果仍必须通过上述字幕质量校验，且审校记录必须能追溯到全部原始转写、检测标签、术语表命中和裁决结果。
