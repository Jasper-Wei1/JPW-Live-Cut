# 变更记录

## 2026-07-29 - v1.1.3

### Windows 原生冒烟修复

- Whisper 回退预检允许并裁切最多 1.5 秒的尾部时间码偏移；超出该范围仍会拒绝，避免 Windows 上正常的 Whisper.cpp 结尾量化误差中断转录。
- 新增时间码容差边界测试，确保超出允许范围的结果仍不能进入逐字稿。

## 2026-07-29 - v1.1.2

### Windows 原生冒烟修复

- Whisper 回退预检生成的临时 WAV 改用 ASCII 序号文件名，避免 Windows Whisper.cpp 无法读取含中文标签的临时路径。
- 新增回归测试，保证预检临时文件名不依赖中文范围标签。

## 2026-07-29 - v1.1.1

### Windows 原生冒烟修复

- Windows 原生冒烟工作流改为显式调用 `whisper:setup` 与 `whisper:transcribe`，不再将旧的 Whisper 模型参数传给默认 Qwen 入口。
- 新增回归测试，确保该工作流始终只验证明确请求的 Whisper 回退路径。

## 2026-07-28 - v1.1.0

### Qwen 审校工作流与 Windows 发布包

- 新增逐字稿审校包：通用大模型只能提出可追溯的原文精确匹配、连续且等长度的字符替换；无法确认的内容保留 Qwen 原文并记录到 `ambiguities`。
- 切点确认前完成审校，锁定母版后复用同一份已确认结果生成最终字幕；Studio 只确认最终 9:16 视觉。
- 根据可用 CPU 和内存自动推导 Qwen/Remotion 并发，避免把单台设备的运行参数写死。
- 发布 Windows ZIP，包含可安装的项目文件、演示素材和 Windows 指引；不包含模型、依赖、用户媒体或工作区数据。Windows Qwen CPU 路径仍待真实设备的完整媒体验证。
- 模板画廊固定使用仓库内演示资源，真实切片仅通过 Workflow 当前数据加载，避免将本机生成媒体带入发布或 CI。

## 2026-07-27

### 本地 Qwen 默认转录

- 默认转录改为本地 `Qwen3-ASR-0.6B + Qwen3-ForcedAligner-0.6B`，运行时保持离线；Whisper.cpp 仅保留为显式回退。
- 整场直播只运行一次 Qwen，保留强制对齐的时间戳来源和精度标记；已批准连续切片从该时间轴重映射，不再多模型复转写。
- 最终字幕只允许依据原声做窄审校；不能确认的文字保留原始 Qwen 输出并写入 `ambiguities`。
- 当前默认运行范围为 macOS MPS，未宣称 Windows Qwen 支持。

## 2026-07-26

### 本地逐字稿审校策略

- 全片初稿固定使用本地 Whisper.cpp `small`，作为候选评分的完整时间轴证据。
- Studio 批准并锁定的连续区间，使用 `medium` 从派生母版原声轨复转写；文本模型只负责发现差异和风险。
- 风险句上下文不超过 30 秒时，才使用 `large-v3-turbo` 进行第三次本地转写，三种模型均与 Studio 和渲染顺序执行，面向 16GB 设备运行。
- 文本模型只能在已有语音识别结果与已批准术语表中裁决，不能自由改写字幕；无法裁决时保留 `medium` 结果并写入 `ambiguities`。
