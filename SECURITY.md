# 安全政策

## 支持范围

目前只为最新 Release 和 `main` 分支提供安全修复。旧版本发现问题后，请先确认最新
版本是否仍可复现。

## 报告漏洞

请不要在公开 Issue 中提交可利用细节、个人媒体、逐字稿、访问令牌或本机路径。

优先使用仓库 Security 页面中的 **Report a vulnerability** 私密报告入口：
<https://github.com/Jasper-Wei1/JPW-Live-Cut/security/advisories/new>。
如果入口不可用，可新建一个不含漏洞细节的 Issue，请维护者提供私密联系方式。

报告请包含受影响版本、平台、最小复现步骤、实际影响和建议修复方式。维护者确认收到后，
会在完成影响评估后回复处理计划。

## 数据边界

原始媒体、逐字稿、缓存和输出默认被 Git 忽略。提交问题前仍应主动检查附件和日志，
删除任何不希望公开的媒体内容、文本、用户名和绝对路径。
